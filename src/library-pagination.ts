// ABOUTME: Defines the bounded Library render batch used by the renderer and its explicit load-more control.
// ABOUTME: Keeps large archives searchable as a whole without mounting every poster card in one blocking commit.
export const libraryBatchSize = 120;
